//user context to manage state for users
//need to add handleSumbit form handler that includes axios.post to login and signup forms

import { createContext } from 'react';

export default createContext(null);
